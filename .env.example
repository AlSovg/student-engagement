# База данных PostgreSQL (Neon, Supabase или локальная)
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# NextAuth — сгенерировать: npx auth secret
AUTH_SECRET=""
AUTH_URL="http://localhost:3000"
